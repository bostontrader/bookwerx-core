//const KeysTest = function (client, testdata) {
  //this.client = client
  //this.testdata = testdata
//}

//KeysTest.prototype.testRunner = function (pn, testdata, priorResults) {

    //const collectionPlural = 'keys'

    // now POST a new
    //.then(priorResults => {
      //return this.post({}, collectionPlural, pn, true, priorResults) // expect success
    //})


//}



  // POST /{collectionPlural}
//KeysTest.prototype.post = function (document, collectionPlural, pn, fExpectSuccess, priorResults) {
    //return new Promise((resolve, reject) => {
      //const url = '/' + collectionPlural
      //console.log('P%s.2 POST %s %j', pn, url, document)
      //this.client.post(url, document, function (err, req, res, obj) {
        //if (err) reject(err)
        //if (!fExpectSuccess && !obj.error) reject('this test must generate an error')
        //if (fExpectSuccess && !obj._id) reject('this test must generate an _id')
        //console.log('P%s.2 %j', pn, obj)
        //if (fExpectSuccess) priorResults[collectionPlural].push(obj)
        //resolve(priorResults)
      //})
    //})
  //}


//export default KeysTest